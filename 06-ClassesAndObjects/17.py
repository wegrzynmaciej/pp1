from classes import Fraction

fraction = Fraction()
fraction.create_fraction(1, 2)
fraction.show_fraction()
fraction.simplify_fraction()
print('Po uproszczeniu')
fraction.show_fraction()
print('--')
fraction.create_fraction(12, 21)
fraction.show_fraction()
fraction.simplify_fraction()
print('Po uproszczeniu')
fraction.show_fraction()
